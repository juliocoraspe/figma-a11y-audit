/** Sandbox-side re-exports of the shared message contract. */
export type {
  SandboxToUI,
  UIToSandbox,
  ScanScope,
} from "@shared/types/Message";
export { assertNever } from "@shared/types/Message";
