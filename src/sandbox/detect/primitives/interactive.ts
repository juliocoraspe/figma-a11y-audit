/**
 * Interactivity classifier — STUB for Phase 1.
 *
 * Phase 2 will use this for tap-target and focus checks. The eventual logic
 * will inspect: name conventions ("btn", "button", "cta"), Figma reactions,
 * and component metadata. Not used by check 01.
 */

import type { NodeShape } from "@shared/types/NodeShape";

export function isInteractive(_node: NodeShape): boolean {
  return false;
}
